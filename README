To test out the library run the driver program by opening the file
picture_language.html in a browser that supports the HTML5 canvas element.

You can also try out the javascript Rhino version be entering the command:
jrunscript driver.js

